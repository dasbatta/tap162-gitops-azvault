tap_install:
  version:
    package_repo_bundle_tag: "1.7.5" # Populate these values with the latest patch version.
    package_version: "1.7.5"
  package_repository:
    oci_repository: tapazreg.azurecr.io/tap174-images/tap-packages
